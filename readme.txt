Quick setup — if you’ve done this kind of thing before
or	
https://github.com/Faysal179038/100-Days-of-Machine-Learning.git
Get started by creating a new file or uploading an existing file. We recommend every repository include a README, LICENSE, and .gitignore.

…or create a new repository on the command line
echo "# 100-Days-of-Machine-Learning" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Faysal179038/100-Days-of-Machine-Learning.git
git push -u origin main
…or push an existing repository from the command line
git remote add origin https://github.com/Faysal179038/100-Days-of-Machine-Learning.git
git branch -M main
git push -u origin main

################################ MY EDIT ##############################

# Open Git Bash and navigate to the directory
cd "/d/learning/100 Days of Machine Learning"

# Initialize the Git repository if not already done
git init

# Add all new folders and files to the staging area
git add .

# Commit the changes with a message
git commit -m "Add all new folders and files"

# Add the remote repository (only if not already added)
git remote add origin https://github.com/username/repository.git

# Push the changes to the remote repository
git push -u origin master
